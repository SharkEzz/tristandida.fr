import type { ComponentStyleConfig } from '@chakra-ui/theme';

const Text: ComponentStyleConfig = {
  baseStyle: {},
  defaultProps: {},
  sizes: {},
  variants: {
    accent: {
      color: 'teal.200',
    },
  },
};

export default Text;
